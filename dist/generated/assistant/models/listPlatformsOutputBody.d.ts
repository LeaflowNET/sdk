/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Leaflow Assistant 用户 API
 * 助手对话及其接入面：线程、逐字稿、一次 turn 的执行与流式输出、工具调用与审批、追问、技能，以及通道、绑定和微信扫码登录。

## 认证

所有接口都要求 `Authorization: Bearer <项目令牌>`。项目令牌向 `POST /api/v1/projects/{projectId}/token` 换取，登录服务是 auth.leaflow.net。一张项目令牌同时确定当前用户和当前项目，因此路径上不带 `project_id`。

令牌无效或过期返回 401，不是当前项目的成员返回 403。这两种都应重新换取令牌后重试；仍然失败说明这个账号在该项目里已没有权限。

## 一次对话

1. `POST /v1/threads` 创建线程；
2. `POST /v1/threads/{thread}/messages` 发送消息。它立即返回 `turnId`，不等待执行完成——一次 turn 可能持续数十分钟；
3. `GET /v1/threads/{thread}` 取回线程文档。文档中的 `stream` 给出实时输出的地址和入场票据；流推送的是对该文档的增量编辑，客户端渲染逻辑只有一套；
4. turn 需要人工介入时，文档中的 `wait` 说明介入类型：批准一批工具调用（`POST /v1/threads/{thread}/approvals/{batch}`），或回答提出的问题（`POST /v1/threads/{thread}/questions/{item}`）。

图片分两步发送：先 `POST /v1/attachments` 取得附件 id，再在发送消息时放进 `attachmentIds`。
 * OpenAPI spec version: 1.0.0
 */
import type { ListPlatformsOutputBodyPlatforms } from './listPlatformsOutputBodyPlatforms.js';
export interface ListPlatformsOutputBody {
    platforms: ListPlatformsOutputBodyPlatforms;
}
