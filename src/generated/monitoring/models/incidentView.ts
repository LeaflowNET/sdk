/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Leaflow 监控 用户 API
 * 用户自己项目里的监控：接入机器、看指标、处理告警、开维护窗口、定可用率目标。

## 怎么带身份

每个请求都要带 `Authorization: Bearer <项目令牌>`。先在 auth.leaflow.net 登录拿到账号令牌，再用它向 IAM 换取项目令牌（`POST /api/v1/projects/{projectId}/token`）。一张项目令牌同时说明当前用户和当前项目。

**路径上没有 `projectId`**：当前项目由令牌决定。

失败时按出路区分：`IDENTITY_EXPIRED` 表示令牌过期，重新换取即可，无需重新登录；`PROJECT_FORBIDDEN` 表示当前账号不是该项目成员；`PROJECT_UNAVAILABLE` 表示项目欠费或正在删除。

## 两组容易混淆的概念

- **停止监控不等于删除**：`POST /servers/{id}/disable` 可逆，历史数据完整保留；`DELETE /servers/{id}` 会连同监控主机、历史数据和这台机器名下的告警一并删除，不可恢复。
- **告警恢复不等于告警了结**：`incident_status` 表示监控系统是否判定已恢复，`closed_at` 表示是否有人完成了处理。尚未恢复的告警也可以按「已接受的风险」了结。

## PSK 只返回一次

接入和轮换的响应中包含 `tls_psk`，其他任何接口都不会再返回它，请及时保存。遗失后可再轮换一次，代价是被监控的机器需要同步更新一次 agent 配置。
 * OpenAPI spec version: 1.0.0
 */
import type { IncidentViewAcknowledgedAt } from './incidentViewAcknowledgedAt.js';
import type { IncidentViewAssignedAt } from './incidentViewAssignedAt.js';
import type { IncidentViewClosedAt } from './incidentViewClosedAt.js';
import type { IncidentViewFollowers } from './incidentViewFollowers.js';
import type { IncidentViewIncidentStatus } from './incidentViewIncidentStatus.js';
import type { IncidentViewItems } from './incidentViewItems.js';
import type { IncidentViewResolvedAt } from './incidentViewResolvedAt.js';
import type { IncidentViewSeverity } from './incidentViewSeverity.js';
import type { IncidentViewTags } from './incidentViewTags.js';

export interface IncidentView {
  acknowledge_message: string;
  acknowledged: boolean;
  acknowledged_at: IncidentViewAcknowledgedAt;
  acknowledged_by_display_name: string;
  acknowledged_by_user_id: string;
  assigned_at: IncidentViewAssignedAt;
  assignee_display_name: string;
  assignee_user_id: string;
  close_message: string;
  close_reason: string;
  closed_at: IncidentViewClosedAt;
  closed_by_display_name: string;
  closed_by_user_id: string;
  duration_seconds: number;
  followers: IncidentViewFollowers;
  host_connection: string;
  host_display_name: string;
  id: string;
  /** 监控系统是否判定已恢复。它与是否有人了结这条告警是两件事 */
  incident_status: IncidentViewIncidentStatus;
  items: IncidentViewItems;
  name: string;
  /** 触发器配的那行现场数据 */
  operational_data: string;
  project_id: string;
  resolved_at: IncidentViewResolvedAt;
  resolved_trigger_expression: string;
  server_id: string;
  server_name: string;
  severity: IncidentViewSeverity;
  started_at: string;
  tags: IncidentViewTags;
  trigger_expression: string;
  zabbix_event_id: string;
}
