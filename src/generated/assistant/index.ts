/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Leaflow Assistant 用户 API
 * 助手对话及其接入面：线程、逐字稿、一次 turn 的执行与流式输出、工具调用与审批、追问、技能，以及通道、绑定和微信扫码登录。

## 认证

所有接口都要求 `Authorization: Bearer <项目令牌>`。项目令牌向 `POST /api/v1/projects/{projectId}/token` 换取，登录服务是 auth.leaflow.net。一张项目令牌同时确定当前用户和当前项目，因此路径上不带 `project_id`。

令牌无效或过期返回 401，不是当前项目的成员返回 403。这两种都应重新换取令牌后重试；仍然失败说明这个账号在该项目里已没有权限。

## 一次对话

1. `POST /v1/threads` 创建线程；
2. `POST /v1/threads/{thread}/messages` 发送消息。它立即返回 `turnId`，不等待执行完成——一次 turn 可能持续数十分钟；
3. `GET /v1/threads/{thread}` 取回线程文档。文档中的 `stream` 给出实时输出的地址和入场票据；流推送的是对该文档的增量编辑，客户端渲染逻辑只有一套；
4. turn 需要人工介入时，文档中的 `wait` 说明介入类型：批准一批工具调用（`POST /v1/threads/{thread}/approvals/{batch}`），或回答提出的问题（`POST /v1/threads/{thread}/questions/{item}`）。

图片分两步发送：先 `POST /v1/attachments` 取得附件 id，再在发送消息时放进 `attachmentIds`。
 * OpenAPI spec version: 1.0.0
 */
import type {
  AnswerInputBody,
  ChannelView,
  ClaimCodeOutputBody,
  CreateChannelInputBody,
  CreateThreadInputBody,
  DecideInputBody,
  DocumentView,
  ListBindingsOutputBody,
  ListBindingsParams,
  ListChannelRejectionsParams,
  ListChannelsOutputBody,
  ListChannelsParams,
  ListModelsOutputBody,
  ListPlatformsOutputBody,
  ListRejectionsOutputBody,
  ListThreadsOutputBody,
  ListThreadsParams,
  LoginView,
  RevertInputBody,
  RevertOutputBody,
  RotateSecretOutputBody,
  SendMessageInputBody,
  SendMessageOutputBody,
  ThreadSummaryView,
  UpdateChannelInputBody,
  UpdateThreadInputBody,
  UploadedView,
  VerifyCodeInputBody
} from './models/index.js';

import { request } from '../../http.js';



  /**
 * 请求体直接是文件字节，不使用 multipart 封装，一次上传一个文件。类型由内容判定，与 Content-Type 无关。返回的 id 在发送消息时放进 attachmentIds；从未被任何消息引用的附件会被定期清除。
 * @summary 上传图片
 */
export const uploadAttachment = (
    uploadAttachmentBody: Blob,
 ) => {
      return request<UploadedView>(
      {url: `/v1/attachments`, method: 'POST',
      headers: {'Content-Type': 'application/octet-stream', },
      data: uploadAttachmentBody
    },
      );
    }
  
/**
 * 按附件 id 取回原始字节，可直接作为 <img> 的地址使用。响应带长期缓存头，附件内容不会变化。附件不存在或不属于当前用户时返回 404。
 * @summary 取回图片
 */
export const downloadAttachment = (
    attachment: string,
 ) => {
      return request<void>(
      {url: `/v1/attachments/${attachment}`, method: 'GET'
    },
      );
    }
  
/**
 * @summary 列出绑定
 */
export const listBindings = (
    params?: ListBindingsParams,
 ) => {
      return request<ListBindingsOutputBody>(
      {url: `/v1/bindings`, method: 'GET',
        params
    },
      );
    }
  
/**
 * @summary 解除绑定
 */
export const deleteBinding = (
    binding: string,
 ) => {
      return request<void>(
      {url: `/v1/bindings/${binding}`, method: 'DELETE'
    },
      );
    }
  
/**
 * @summary 列出通道
 */
export const listChannels = (
    params?: ListChannelsParams,
 ) => {
      return request<ListChannelsOutputBody>(
      {url: `/v1/channels`, method: 'GET',
        params
    },
      );
    }
  
/**
 * @summary 创建通道
 */
export const createChannel = (
    createChannelInputBody: CreateChannelInputBody,
 ) => {
      return request<ChannelView>(
      {url: `/v1/channels`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: createChannelInputBody
    },
      );
    }
  
/**
 * 删除后该通道不再接收入站消息，其上的绑定一并失效。项目处于停服或清理状态时本接口仍然可用。
 * @summary 删除通道
 */
export const deleteChannel = (
    channel: string,
 ) => {
      return request<void>(
      {url: `/v1/channels/${channel}`, method: 'DELETE'
    },
      );
    }
  
/**
 * @summary 查看通道
 */
export const getChannel = (
    channel: string,
 ) => {
      return request<ChannelView>(
      {url: `/v1/channels/${channel}`, method: 'GET'
    },
      );
    }
  
/**
 * @summary 修改通道
 */
export const updateChannel = (
    channel: string,
    updateChannelInputBody: UpdateChannelInputBody,
 ) => {
      return request<ChannelView>(
      {url: `/v1/channels/${channel}`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: updateChannelInputBody
    },
      );
    }
  
/**
 * 生成一个一次性认领码交给待绑定的人，他在该平台上用自己的账号把这个码发给助手即完成绑定。绑定只能由本人以这种方式建立，不能直接指定平台账号。认领码有有效期，过期后需重新签发。
 * @summary 签发认领码
 */
export const createClaimCode = (
    channel: string,
 ) => {
      return request<ClaimCodeOutputBody>(
      {url: `/v1/channels/${channel}/claim-codes`, method: 'POST'
    },
      );
    }
  
/**
 * 排查「发了消息但助手没有响应」时使用。按时间倒序返回最近被这条通道拒绝的入站消息及其拒绝原因，最常见的原因是发送方尚未绑定。
 * @summary 查看最近被拒绝的入站消息
 */
export const listChannelRejections = (
    channel: string,
    params?: ListChannelRejectionsParams,
 ) => {
      return request<ListRejectionsOutputBody>(
      {url: `/v1/channels/${channel}/rejections`, method: 'GET',
        params
    },
      );
    }
  
/**
 * 生成新的回调密钥并立即使旧密钥失效。新密钥仅在本次响应中返回，之后无法再次取回，请先在平台侧完成配置。
 * @summary 轮换回调密钥
 */
export const rotateChannelSecret = (
    channel: string,
 ) => {
      return request<RotateSecretOutputBody>(
      {url: `/v1/channels/${channel}/secret`, method: 'POST'
    },
      );
    }
  
/**
 * 微信个人号通道需要本人扫码登录后才能收发消息。本接口返回二维码，之后轮询 `GET /v1/weixin-logins/{login}` 获取进度；状态提示需要验证码时，调用 `POST /v1/weixin-logins/{login}/verify-code` 补交。
 * @summary 发起微信扫码登录
 */
export const beginWeixinLogin = (
    channel: string,
 ) => {
      return request<LoginView>(
      {url: `/v1/channels/${channel}/weixin-logins`, method: 'POST'
    },
      );
    }
  
/**
 * 返回本平台当前提供的模型及其上下文窗口、推理档位和支持的输入类型。用于填充对话设置里的模型选择。清单与项目无关，但仍需要有效的项目令牌。
 * @summary 列出可用模型
 */
export const listModels = (
    
 ) => {
      return request<ListModelsOutputBody>(
      {url: `/v1/models`, method: 'GET'
    },
      );
    }
  
/**
 * 返回本平台当前支持接入的即时通讯平台，以及各自建通道时需要提供的凭据字段。用于填充新建通道表单。
 * @summary 列出可接入的平台
 */
export const listPlatforms = (
    
 ) => {
      return request<ListPlatformsOutputBody>(
      {url: `/v1/platforms`, method: 'GET'
    },
      );
    }
  
/**
 * @summary 列出对话
 */
export const listThreads = (
    params?: ListThreadsParams,
 ) => {
      return request<ListThreadsOutputBody>(
      {url: `/v1/threads`, method: 'GET',
        params
    },
      );
    }
  
/**
 * @summary 创建对话
 */
export const createThread = (
    createThreadInputBody: CreateThreadInputBody,
 ) => {
      return request<ThreadSummaryView>(
      {url: `/v1/threads`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: createThreadInputBody
    },
      );
    }
  
/**
 * 对话的完整当前状态，用于首屏渲染。文档中的 stream 给出实时输出地址和入场票据，流推送的是对这份文档的增量编辑，可直接套用同一套渲染逻辑。
 * @summary 取回对话文档
 */
export const getThread = (
    thread: string,
 ) => {
      return request<DocumentView>(
      {url: `/v1/threads/${thread}`, method: 'GET'
    },
      );
    }
  
/**
 * 可修改模型、推理档位、审批模式和归档状态。改动从下一次 turn 起生效，正在执行的 turn 沿用它启动时的设置。reasoningEffort 仅在同时提供 model 时生效。
 * @summary 修改对话设置
 */
export const updateThread = (
    thread: string,
    updateThreadInputBody: UpdateThreadInputBody,
 ) => {
      return request<ThreadSummaryView>(
      {url: `/v1/threads/${thread}`, method: 'PATCH',
      headers: {'Content-Type': 'application/json', },
      data: updateThreadInputBody
    },
      );
    }
  
/**
 * 批次 id 来自对话文档的 wait 字段。本接口是幂等的：重复提交同一批次不会改变已经生效的决定，也不会报错。批次不属于该对话时返回 404。
 * @summary 批准或拒绝一批工具调用
 */
export const decideApproval = (
    thread: string,
    batch: string,
    decideInputBody: DecideInputBody,
 ) => {
      return request<void>(
      {url: `/v1/threads/${thread}/approvals/${batch}`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: decideInputBody
    },
      );
    }
  
/**
 * 对没有正在执行的 turn 的对话调用同样返回 204，不视为错误——用户点击停止与 turn 自然结束之间存在竞争，两种结果一致。项目处于停服或清理状态时本接口仍然可用。
 * @summary 中断正在执行的 turn
 */
export const interruptThread = (
    thread: string,
 ) => {
      return request<void>(
      {url: `/v1/threads/${thread}/interrupt`, method: 'POST'
    },
      );
    }
  
/**
 * 立即返回 turnId，不等待执行完成——一次 turn 可能持续数十分钟。执行进度通过对话文档中 stream 指向的实时流获取，不在本响应里。
 * @summary 发送消息并触发一次 turn
 */
export const sendMessage = (
    thread: string,
    sendMessageInputBody: SendMessageInputBody,
 ) => {
      return request<SendMessageOutputBody>(
      {url: `/v1/threads/${thread}/messages`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: sendMessageInputBody
    },
      );
    }
  
/**
 * 问题 id 来自对话文档的 wait 字段。已被回答过的问题同样返回 204——可能是另一个页面提交在先，也可能是自动应答窗口已到期，两种情况下 turn 都已带着答案继续执行。
 * @summary 回答助手提出的问题
 */
export const answerQuestion = (
    thread: string,
    item: string,
    answerInputBody: AnswerInputBody,
 ) => {
      return request<void>(
      {url: `/v1/threads/${thread}/questions/${item}`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: answerInputBody
    },
      );
    }
  
/**
 * @summary 标记对话已读
 */
export const markThreadRead = (
    thread: string,
 ) => {
      return request<void>(
      {url: `/v1/threads/${thread}/read`, method: 'POST'
    },
      );
    }
  
/**
 * 撤回 ordinal 及其之后的全部条目。被撤回的条目仍留在逐字稿中并标记 reverted，序号不会重排。返回实际撤回的条目数。
 * @summary 从指定位置起撤回
 */
export const revertThread = (
    thread: string,
    revertInputBody: RevertInputBody,
 ) => {
      return request<RevertOutputBody>(
      {url: `/v1/threads/${thread}/revert`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: revertInputBody
    },
      );
    }
  
/**
 * 轮询本接口直到状态变为成功或失败。状态提示需要验证码时，调用补交验证码接口。
 * @summary 查询扫码登录状态
 */
export const getWeixinLogin = (
    login: string,
 ) => {
      return request<LoginView>(
      {url: `/v1/weixin-logins/${login}`, method: 'GET'
    },
      );
    }
  
/**
 * 微信在扫码后要求短信或设备验证码时使用。验证码由登录发起人在自己手机上获取。
 * @summary 补交登录验证码
 */
export const submitWeixinVerifyCode = (
    login: string,
    verifyCodeInputBody: VerifyCodeInputBody,
 ) => {
      return request<LoginView>(
      {url: `/v1/weixin-logins/${login}/verify-code`, method: 'POST',
      headers: {'Content-Type': 'application/json', },
      data: verifyCodeInputBody
    },
      );
    }
  
export type UploadAttachmentResult = NonNullable<Awaited<ReturnType<typeof uploadAttachment>>>
export type DownloadAttachmentResult = NonNullable<Awaited<ReturnType<typeof downloadAttachment>>>
export type ListBindingsResult = NonNullable<Awaited<ReturnType<typeof listBindings>>>
export type DeleteBindingResult = NonNullable<Awaited<ReturnType<typeof deleteBinding>>>
export type ListChannelsResult = NonNullable<Awaited<ReturnType<typeof listChannels>>>
export type CreateChannelResult = NonNullable<Awaited<ReturnType<typeof createChannel>>>
export type DeleteChannelResult = NonNullable<Awaited<ReturnType<typeof deleteChannel>>>
export type GetChannelResult = NonNullable<Awaited<ReturnType<typeof getChannel>>>
export type UpdateChannelResult = NonNullable<Awaited<ReturnType<typeof updateChannel>>>
export type CreateClaimCodeResult = NonNullable<Awaited<ReturnType<typeof createClaimCode>>>
export type ListChannelRejectionsResult = NonNullable<Awaited<ReturnType<typeof listChannelRejections>>>
export type RotateChannelSecretResult = NonNullable<Awaited<ReturnType<typeof rotateChannelSecret>>>
export type BeginWeixinLoginResult = NonNullable<Awaited<ReturnType<typeof beginWeixinLogin>>>
export type ListModelsResult = NonNullable<Awaited<ReturnType<typeof listModels>>>
export type ListPlatformsResult = NonNullable<Awaited<ReturnType<typeof listPlatforms>>>
export type ListThreadsResult = NonNullable<Awaited<ReturnType<typeof listThreads>>>
export type CreateThreadResult = NonNullable<Awaited<ReturnType<typeof createThread>>>
export type GetThreadResult = NonNullable<Awaited<ReturnType<typeof getThread>>>
export type UpdateThreadResult = NonNullable<Awaited<ReturnType<typeof updateThread>>>
export type DecideApprovalResult = NonNullable<Awaited<ReturnType<typeof decideApproval>>>
export type InterruptThreadResult = NonNullable<Awaited<ReturnType<typeof interruptThread>>>
export type SendMessageResult = NonNullable<Awaited<ReturnType<typeof sendMessage>>>
export type AnswerQuestionResult = NonNullable<Awaited<ReturnType<typeof answerQuestion>>>
export type MarkThreadReadResult = NonNullable<Awaited<ReturnType<typeof markThreadRead>>>
export type RevertThreadResult = NonNullable<Awaited<ReturnType<typeof revertThread>>>
export type GetWeixinLoginResult = NonNullable<Awaited<ReturnType<typeof getWeixinLogin>>>
export type SubmitWeixinVerifyCodeResult = NonNullable<Awaited<ReturnType<typeof submitWeixinVerifyCode>>>
