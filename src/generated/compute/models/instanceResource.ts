/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Leaflow 计算用户 API
 * Leaflow 计算的用户接口，用于管理云服务器、云硬盘、快照、备份、自制镜像、私有网络、子网、路由、安全组、网卡与公网 IP。

## 身份与鉴权

每个请求需携带 `Authorization: Bearer <项目令牌>`。先在 auth.leaflow.net 登录获取账号令牌，再用它向 IAM 换取项目令牌（`POST /api/v1/projects/{projectId}/token`）。一张项目令牌同时说明当前用户和当前项目。

**路径和请求头上都没有 `project_id`**：当前项目由令牌决定。

鉴权在网关完成，失败的请求不会到达这些接口。按错误码区分处理方式：`TOKEN_MISSING` 表示没带令牌；`TOKEN_EXPIRED` 表示令牌过期，用账号令牌重新换取即可，无需重新登录；`TOKEN_INVALID` 表示令牌验不过；`NOT_A_MEMBER` 表示当前账号不是该项目成员；`USER_SUSPENDED` / `USER_BANNED` 表示账号本身不能操作。

## 通用约定

- **操作是异步的。** 创建云服务器、创建云硬盘、变更配置在返回时尚未完成，资源处于 `provisioning` 等中间状态，需轮询 GET 确认结果。查询单个资源时会同步一次当前状态。
- **可用区。** 云硬盘与云服务器必须位于同一可用区才能挂载。机型和硬盘类型各自绑定在一个可用区上，选定类型即选定可用区。
- **IPv6 不单独售卖。** 在私有网络上启用后，IPv6 地址会自动下发至该网络内的云服务器，无需也无法单独申领。
- **公网 IP 有冷却期。** 释放后短时间内既不能重新申领，也不会分配给其他用户，以免仍指向该地址的 DNS 记录和访问白名单立即失效。
- **密码只返回一次。** 创建或重装时若由平台生成密码，它仅出现在该次响应中，请及时保存。
 * OpenAPI spec version: 1.0.0
 */
import type { InstanceResourceImageId } from './instanceResourceImageId.js';
import type { InstanceResourceIpv6Address } from './instanceResourceIpv6Address.js';
import type { InstanceResourcePendingInstanceTypeId } from './instanceResourcePendingInstanceTypeId.js';
import type { InstanceResourcePrivateImageId } from './instanceResourcePrivateImageId.js';
import type { InstanceResourcePrivateIp } from './instanceResourcePrivateIp.js';
import type { InstanceResourcePrivateNetworkId } from './instanceResourcePrivateNetworkId.js';
import type { InstanceResourcePublicIps } from './instanceResourcePublicIps.js';
import type { InstanceResourceStatus } from './instanceResourceStatus.js';
import type { InstanceResourceSubnetId } from './instanceResourceSubnetId.js';
import type { InstanceResourceSuspendedAt } from './instanceResourceSuspendedAt.js';

export interface InstanceResource {
  availability_zone: string;
  created_at: string;
  /** 云服务器内的主机名，等于云服务器 id */
  hostname: string;
  id: string;
  /** 从平台提供的镜像创建时非空 */
  image_id: InstanceResourceImageId;
  /** 当前生效的机型，即当前计费依据 */
  instance_type_id: string;
  /** 云服务器主网卡的 IPv6 地址。私有网络启用 IPv6 后自动下发 */
  ipv6_address: InstanceResourceIpv6Address;
  name: string;
  /** 非空表示存在待确认的变配。确认后该机型生效，回滚则丢弃 */
  pending_instance_type_id: InstanceResourcePendingInstanceTypeId;
  /** 从自制的私有镜像创建时非空 */
  private_image_id: InstanceResourcePrivateImageId;
  /** 云服务器的内网地址 */
  private_ip: InstanceResourcePrivateIp;
  /** 云服务器主网卡所在的私有网络 */
  private_network_id: InstanceResourcePrivateNetworkId;
  /** 云服务器主网卡上绑定的公网 IPv4，未绑定时为空数组 */
  public_ips: InstanceResourcePublicIps;
  region_code: string;
  status: InstanceResourceStatus;
  /** 云服务器主网卡所在的子网 */
  subnet_id: InstanceResourceSubnetId;
  /** 非空表示已被平台停服，需先解除后才能操作 */
  suspended_at: InstanceResourceSuspendedAt;
  updated_at: string;
}
